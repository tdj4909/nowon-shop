# Gradle 데몬 JVM 힙 상한
#
# 지정하지 않으면 Gradle이 호스트 메모리를 기준으로 힙을 잡는데,
# Railway 같은 컨테이너 빌더에서는 cgroup 제한보다 큰 값을 잡아 OOM으로 죽는다.
# 이 프로젝트 규모에서는 512m로 충분하다.
org.gradle.jvmargs=-Xmx512m -XX:MaxMetaspaceSize=256m

# 단일 모듈이라 병렬 실행의 이득이 없다. 워커를 늘리면 빌더 메모리만 더 쓴다.
org.gradle.parallel=false
